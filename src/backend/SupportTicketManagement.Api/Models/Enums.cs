namespace SupportTicketManagement.Api.Models;

public enum TicketPriority
{
    Low = 0,
    Medium = 1,
    High = 2
}

public enum TicketStatus
{
    Open = 0,
    InProgress = 1,
    Resolved = 2,
    Closed = 3,
    Cancelled = 4
}

